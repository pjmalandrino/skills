---
description: Check staged/unstaged changes against commit conventions
---
Read @engineering/dev-flow/rules/commit-conventions.md.

Run `git status` and `git diff --staged` to see what is about to be committed.
Propose one or more commit messages that follow the conventions, grouped logically if the diff covers multiple concerns.
Do not run `git commit` — just propose the messages.
